# 危机检测与应对功能说明

## 功能概述

系统现已集成**自动危机信号检测**机制，当用户表达极端或自杀想法时，AI 助手会自动触发专业应对流程。

## 核心改进

### 1. 危机信号关键词库
系统能够识别以下关键词，自动进入危机应对模式：

**高危信号词汇：**
- 自杀、自伤、割腕、喝药、跳楼、死亡
- 没活着的意义、生不如死、没有希望
- 无法承受、完全崩溃、无法活下去
- 放弃生命、结束生命、走极端、绝望
- 活不了、负担、累赘、别人的负担
- 太痛苦、无法忍受、每天都是折磨、深度抑郁、完全失控

### 2. 自动危机检测流程

当用户消息包含危机关键词时：

1. **系统自动识别** → 后台日志打印 `📢 检测到危机信号`
2. **用户界面提示** → 前端显示 `[系统提醒] 我检测到您可能处于困难的情境。让我们一起寻求帮助...`
3. **触发增强提示词** → AI 使用专门的危机应对提示词模板
4. **温暖而坚定的响应** → AI 会：
   - 表达真诚的关心与担忧
   - 确认用户感受的有效性
   - **强烈建议申请与心理专家对话**
   - 给出具体行动方案

### 3. 增强的提示词 (CRISIS_PROMPT_EXTENSION)

危机模式下，系统会附加以下指导：

```
⚠️  【紧急：用户可能处于危机状态】
如果用户的消息中出现了自杀、自伤或严重绝望的想法，你必须：

1. 立即表达真诚的关心和担忧
2. 让用户知道他们的感受被认真对待
3. 温暖而坚定地建议寻求专业心理咨询师的帮助
4. 提醒用户可以通过【申请与心理专家对话】功能获得专业帮助
5. 如果可能，建议拨打心理援助热线或联系学校心理咨询中心

回复框架：
- 第一句：真诚的关心 "听到这些，我很担心你..."
- 第二句：确认其感受有效 "你的感受很重要，你值得被帮助"
- 第三句：建议专业帮助 "我强烈建议你与我们的心理专家进行对话..."
- 最后：给出具体行动 "你可以点击【申请与心理专家对话】按钮..."
```

### 4. 实现细节

**AiService.java 中的关键方法：**

```java
// 危机信号检测
private boolean detectCrisisSignals(String userMessage)

// 危机感知的流式对话
public void streamChatWithEnhancedPrompt(String userMessage, boolean hasCrisisSignals, ...)

// 带危机检测的消息保存
public void streamChatAndSave(Long sessionId, Long userId, String userMessage, ...)
```

**工作流程：**
1. 用户发送消息 → `streamChatAndSave()` 被调用
2. 系统调用 `detectCrisisSignals()` 检测关键词
3. 如果检测到危机信号 → 使用 `SYSTEM_PROMPT + CRISIS_PROMPT_EXTENSION`
4. 响应 temperature 降低为 0.5（确保一致性）
5. 前端显示系统警告，然后流式输出 AI 回复

## 使用场景示例

### 用户输入：
```
我真的想死，每天都活得很痛苦，我是别人的负担...
```

### 系统响应流程：
1. 后端日志：`[AiService] 📢 检测到危机信号: 想死`
2. 后端日志：`[AiService] 🚨 触发危机应对模式`
3. 前端显示：`[系统提醒] 我检测到您可能处于困难的情境。让我们一起寻求帮助...`
4. 然后流式输出 AI 响应（包含建议申请专家对话）

## 配置参数

### 在 AiService 中可调整：

```java
// 危机模式下的 temperature（0-1，越低越稳定）
body.put("temperature", hasCrisisSignals ? 0.5 : 0.7);

// 危机关键词库（CRISIS_KEYWORDS 数组）
private static final String[] CRISIS_KEYWORDS = { ... };
```

## 前端集成指南

前端需要识别以下特殊标记，以显示不同的 UI 样式：

```javascript
// 检测系统提醒标记
if (message.startsWith("[系统提醒]")) {
    // 显示醒目的警告样式（如红色背景、加粗字体）
    // 同时在界面中突出显示"申请与心理专家对话"按钮
}

// 后续的 AI 回复应该包含建议
if (message.includes("心理专家") || message.includes("申请")) {
    // 在回复中高亮这些部分
    // 使"申请与心理专家对话"按钮可见且易于点击
}
```

## 限制与注意事项

⚠️ **当前限制：**
- 关键词检测是基于中文字符串匹配的简单方案
- 可能存在**漏报**（用户用隐喻表达自杀意图时可能被遗漏）
- 不能替代真实的心理专家评估

✅ **改进方向：**
- 考虑使用 NLP 模型进行更精细的意图识别
- 记录所有危机检测事件，便于后续分析
- 根据实际数据定期更新关键词库

## 测试建议

你可以通过以下方式测试功能：

1. 打开前端，进入 AI 对话页面
2. 输入包含危机关键词的消息，如：
   - "我想自杀"
   - "活着没有意义"
   - "我是别人的负担"
3. 观察：
   - 后端日志是否显示危机检测信息
   - 前端是否显示系统提醒
   - AI 回复是否包含建议申请专家对话

## 后续优化

- [ ] 将危机事件记录到数据库以便审计
- [ ] 为管理员添加危机案例监控面板
- [ ] 集成实时心理援助热线信息
- [ ] 使用 AI 生成的情感分析作为辅助检测
- [ ] 针对不同严重程度的危机提供分级响应

---

**最后更新**: 2026-01-28  
**相关文件**: [AiService.java](psychology-backend/src/main/java/com/example/psychology/service/AiService.java)
